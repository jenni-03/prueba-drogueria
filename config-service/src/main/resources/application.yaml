server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/jenni-03/prueba-drogueria.git
          search-paths: config-data
  application:
    name: config-service


